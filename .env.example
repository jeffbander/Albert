# ===========================================
# Albert Voice Assistant - Environment Config
# ===========================================

# ===========================================
# AI APIs (Required)
# ===========================================

# OpenAI - Required for GPT-4o Realtime voice API
OPENAI_API_KEY=sk-...

# Anthropic - Required for Claude AI capabilities
ANTHROPIC_API_KEY=sk-ant-...

# ===========================================
# Database (Turso/LibSQL)
# ===========================================

# Turso database URL - Get from https://turso.tech
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=

# ===========================================
# Memory (Optional)
# ===========================================

# Mem0 API Key - for persistent memory across sessions
MEM0_API_KEY=

# ===========================================
# Browser Automation
# ===========================================

# Provider: 'local-cdp' for development, 'browserbase' for production
BROWSER_PROVIDER=local-cdp

# Local CDP (development only)
# Launch Chrome with: chrome --remote-debugging-port=9222
CHROME_DEBUG_PORT=9222

# Browserbase (production) - Get from https://browserbase.com
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=

# ===========================================
# Authentication (NextAuth)
# ===========================================

# NextAuth configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Google OAuth - Get from https://console.cloud.google.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth - Get from https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ===========================================
# Gmail Integration (Direct API - Serverless Compatible)
# ===========================================

# Gmail OAuth2 credentials - Get from Google Cloud Console:
# 1. Go to https://console.cloud.google.com
# 2. Create a project or select existing
# 3. Enable Gmail API (APIs & Services > Enable APIs)
# 4. Configure OAuth consent screen
# 5. Create OAuth 2.0 credentials (Web application type)
# 6. Add authorized redirect URI: http://localhost:3000/api/auth/gmail/callback
# 7. Copy Client ID and Client Secret below

GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-client-secret

# Optional: Override the redirect URI (defaults to {APP_URL}/api/auth/gmail/callback)
# GMAIL_REDIRECT_URI=http://localhost:3000/api/auth/gmail/callback

# After setting credentials:
# 1. Start your dev server: npm run dev
# 2. Visit http://localhost:3000/api/auth/gmail?action=authorize
# 3. Complete the Google OAuth flow
# 4. Your tokens will be stored in the database automatically

# ===========================================
# Deployment
# ===========================================

# Environment: development, production, test
NODE_ENV=development

# Base URL for the application (used for callbacks, webhooks)
APP_URL=http://localhost:3000

# ===========================================
# Optional Services
# ===========================================

# Vercel KV (optional - for rate limiting, caching)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Sentry (optional - for error tracking)
SENTRY_DSN=

# Analytics (optional)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=
